const mat = document.getElementById("mat")

//const math = new Math() // Nao precisar ser instanciada pq é static
const num = Math.random() * 100
mat.innerHTML = Math.sqrt(64)
mat.innerHTML = Math.pow(10, 2) // Elevado 
  
