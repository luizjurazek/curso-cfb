// Criar e inicializar um array de 20 posições com valores zero
let vetor = new Array(20).fill(0);

// Imprimir o vetor, indicando a posição e o valor na posição
for (let i = 0; i < vetor.length; i++) {
    console.log(`Posição ${i}: ${vetor[i]}`);
}